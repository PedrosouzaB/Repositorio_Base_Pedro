import flet as ft

def main(page: ft.Page):
    page.title = "Minha Primeira App Flet"
    page.vertical_alignment = ft.MainAxisAlignment.CENTER
    page.add(ft.Text("Olá Mundo!"))

ft.app(target=main)
